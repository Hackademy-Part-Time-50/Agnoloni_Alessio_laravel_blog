


<div class="card border-1 shadow mb-2" >
    <div class="card-header">{{ $title }}</div>

    <div class="card-body">
        <p class="card-text">{{ $articolo }}</p>
        <a href="{{$route}}" class="btn btn-primary">Leggi tutto</a>
    </div>
</div> 
